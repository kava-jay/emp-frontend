variables:
  buildConfiguration: 'Release'

steps:
# - script: nodejs build --configuration $(buildConfiguration)
#   displayName: 'nodejs build $(buildConfiguration)'
# - task: DotNetCoreCLI@2
#   inputs:
#     command: 'publish'
#     publishWebProjects: true
# - task: AzureWebApp@1
#   inputs:
#     azureSubscription: '<Azure service connection>'
#     appType: 'webAppLinux'
#     appName: '<Name of web app>'
#     package: '$(System.DefaultWorkingDirectory)/**/*.zip'
- task: AzureWebApp@1
  inputs:
    azureSubscription: 'TestAzure-devops'
    appType: 'webAppLinux'
    appName: 'empfrontend'
    package: '$(System.DefaultWorkingDirectory)/**/*.zip'
    runtimeStack: 'NODE|18-lts'
# - task: AzureWebApp@1
#   inputs:
#     azureSubscription: 'TestAzure-devops'
#     appType: 'webAppLinux'
#     appName: 'empfrontend'
#     package: '$(System.DefaultWorkingDirectory)/**/*.zip'
- task: NodeTool@0
  inputs:
    versionSource: 'spec'
    versionSpec: '18.x'
- task: Npm@1
  inputs:
    command: 'install'

- task: Npm@1
  inputs:
    command: 'custom'
    customCommand: 'run build'